using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 적군에 부착 돼 있는 hp바가 공격 당하면 당하는 것


public class EnemyParams : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
